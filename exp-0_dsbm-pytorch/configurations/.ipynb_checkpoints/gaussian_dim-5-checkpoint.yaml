# @package _global_

defaults:
  - _self_
#  - job

name: gaussian
seed: 42
a: 3
dim: 5
sigma: 1
num_steps: 20
net_name: mlp_large
activation_fn: torch.nn.SiLU
model_name: dsbm
first_coupling: ref
inner_iters: 10000
outer_iters: 5
fb_sequence: ['b','f']

paths: 

  # Определяем наименование директории в results/
  # для сохранения результатов эксперимента
  # Директория создается автоматически. Если уже есть - сначала удаляется
  experiments_dir_name: gaussian_dim-5_mean-3_var-1